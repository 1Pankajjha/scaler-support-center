{
  "name": "scaler-support-center",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start",
    "install": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {},
  "dependencies": {}
}
